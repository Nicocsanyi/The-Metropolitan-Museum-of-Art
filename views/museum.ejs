<% include header %>
    <div class="container">
        <div class="row">
            <div class="col-md-12 text-center">
                <h3><a href="/">The Metropolitan Museum of Art</a></h3>
                <section>

                    <h4>
                        <%- item.titel %>
                    </h4>
                    <img src="/img/<%- item.afbeeldingl %>" alt="<%- item.titel %>">

                    <p>
                        <%- item.datum %>
                    </p>

                    <p>
                        <%- item.body %>
                    </p>

                    <p>Posted categorieën:

                        <% for (var i = 0; i < item.categorieen.length; i++) { %>
                            <a href="/categorieen/<%- i %>">
                                <%- item.categorieen[i] %>
                            </a>
                            <% if (i+1< item.categorieen.length){ %>,
                                <% } %>
                                    <% } %>
                    </p>
                </section>
            </div>
        </div>
    </div>


    <% include footer %>
